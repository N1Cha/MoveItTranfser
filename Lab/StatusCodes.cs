namespace ConsoleClient
{
    public enum DirectoryStatusCode
    {
        Exists,
        NotFound,
        Error
    }

    public enum LoginStatusCode
    {
        Authorized,
        Unauthorized,
    }
}
